# Đường GPU NVIDIA (SPD-4). Dùng THAY CHO requirements.txt, không dùng thêm.
#
#     pip install -r requirements-gpu.txt
#     QC_SCANNER_ONNX_PROVIDERS=CUDAExecutionProvider,CPUExecutionProvider qc-scanner-server
#
# ⚠️ CHƯA CHẠY THỬ. Máy phát triển là macOS, không có CUDA — file này viết theo tài
# liệu onnxruntime chứ không có bằng chứng thực nghiệm nào. Lần đầu chạy trên máy
# server phải kiểm bằng `/healthz`, xem `providers` có `CUDAExecutionProvider` không.
#
# ⚠️ `onnxruntime` và `onnxruntime-gpu` **xung đột**: cài cả hai thì import lấy bản
# nào là ngẫu nhiên. Gỡ bản CPU trước:  pip uninstall -y onnxruntime
#
# ⚠️ Bản onnxruntime-gpu phải khớp CUDA/cuDNN của máy. Sai bản thì nó **không báo
# lỗi** — chỉ lặng lẽ chạy CPU. Đó là lý do `/healthz` báo cáo provider thật.
# Bảng đối chiếu: https://onnxruntime.ai/docs/execution-providers/CUDA-ExecutionProvider.html

click>=8.1,<9
fastapi>=0.115,<1
imutils>=0.5.4,<0.6
numpy>=1.26,<3
onnxruntime-gpu>=1.17,<2
opencv-python>=4.8,<6
python-multipart>=0.0.9,<1
rembg>=2.0,<3
uvicorn[standard]>=0.30,<1
